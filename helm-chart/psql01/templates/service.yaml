apiVersion: v1
kind: Service
metadata:
  name: {{ include "psql01.fullname" . }}
  labels:
    {{- include "psql01.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "psql01.selectorLabels" . | nindent 4 }}
